services:
  - type: web
    name: quiz-platform-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    env: node
    region: ohio
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
